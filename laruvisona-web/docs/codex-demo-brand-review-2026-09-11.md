# Codex デモ・会社トップ独立レビュー

対象: `2406860` / `1fd1cae`。実装変更なし。本番操作なし。

## 結果

- 単体 444/444 通過。
- `demo-check.mjs` 59/59 通過。
- `brand-visual-check.mjs` 22/22 通過。
- 上記ブラウザ検査はローカル Next.js 開発サーバー。公開用ビルドや本番での性能測定ではない。検査側の外部タグ・Google Fonts 遮断あり。Claude 報告の選択書体込み性能値は独立再計測していない。
- 実ページから PC/スマホの画面を撮影し確認。会社トップの Nano 静止画は採用可。背景との一体感と主役の大きさは納品時の合成より改善。
- 連続選択、選択保持、フォームへの遷移、低モーション時の動作について今回の回帰検査は通過。

## P2: スマホで選択操作と主な変化が同時に見えない

`components/lp/AssembleDemo.tsx` の選択欄がスマホでは見本の後ろに配置され、説明付きの大きなカード3枚が縦に並ぶ。実画面で「やわらかい」を選ぶと、見本の見出し・写真は画面外で、料金欄の下部だけが残る。値は正しく変わっても、「選ぶとその場で変わる」という主な体験が伝わりにくい。

スマホの選択を短い3ラベルのコンパクトな操作にし、見本上部と同じ画面内で触れる配置へ調整する。詳細説明は選択中の1案だけにまとめるなどして初期高さを増やさない。自動スクロールで補わない。PCの配置・状態保持・公開HTML生成処理は維持する。

検証は状態の変更だけでなく、タップ時に操作と見本の主要な変化が画面内で確認できることを見る。今回のフォントを遮断した撮影から、実際の明朝・丸ゴシックの品質は判断しない。

会社画像の追加生成や動画はこの修正に不要。GPT側の原画は保管し、今回は不採用として扱ってよい。

スクリーンショット（今回の開発サーバー、検証用）:
- `/tmp/laru-57-preview/top-pc.png`
- `/tmp/laru-57-preview/top-sp.png`
- `/tmp/laru-57-preview/demo-pc-1-initial.png`
- `/tmp/laru-57-preview/demo-sp-3-picked.png`

push・本番SQL・DNS・デプロイ・本番再生成は未実施。

## 追試: d06d326

スマホの P2 は今回の確認範囲で解消。

- 単体447/447、デモの実ブラウザ検査66/66を独立再実行し通過。
- 別途390×844で3つの選択を個別にタップ。上質／落ち着いた／やわらかいのすべてで見出しの表示位置は約136〜211pxで、画面とクリップ枠の両方に収まることを確認。
- 横3択と変更後の見出し・写真を同じ画面で確認。自動スクロールを追加せず操作と結果が見える。
- ローカル開発サーバー、外部タグ・Google Fonts遮断の検証。公開ビルドでの性能値や実書体の品質を再計測した結果ではない。

当該デモの配置修正は一区切りとし、新たな問題がなければ追加変更は不要。
