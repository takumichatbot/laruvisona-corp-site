# フォーム受付後の通知結果

問い合わせ・簡易予約は、まず `contacts` に保存して受付を確定する。その後の運営メール、自動返信、LINE、Webhookは個別に成否を判定する。

Resendは失敗時に例外ではなく `{ error }` を返す場合があるため、戻り値も確認する。各結果と確認時刻は問い合わせの `extra_fields` へ保存し、Webhook結果の更新時にも他の結果を保持する。APIの `notified` は運営宛メールが実際に受理された場合だけ `true` にする。
