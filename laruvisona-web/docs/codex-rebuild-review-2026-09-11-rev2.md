# Codex：rev2独立レビュー

対象: `248c88f` / `rebuild-2026-09`。2026-09-11。

## 判断

前回の主要な修正は進んだ。素材制作は継続してよい。日本語は当面、会社サイトと案内ページで端末の書体を採用する。顧客が選ぶ書体は維持する。

今回の新しい修正対象は、復元処理と復元対象の範囲。公開計画はブランチの18コミットだけでなく、GitHub mainとの差分全体で作る。素材を待つ間にこれらをまとめて仕上げる。

## 今回独立実行した検証

- 単体: 433/433成功。
- `tests/browser/isolation-check.mjs`: 実Chrome 14/14成功。以前の直列化なら動いてしまう対照も確認。
- `concurrent-save-check.mjs`: 実APIとローカルPostgREST fixtureで13/13成功。本番PostgreSQLの実効権限・トリガまで保証する試験ではない。
- 前回欠けていたpublic/salonの配信画像がGit管理へ移されたことを確認。ローカルHTTPでhero-900.avifとhero-sp-1170.avifがともに200で返った。
- rev2のPC・スマホ画像、変更コード、移行・性能文書を確認。
- R2の公開状態ガード、R3のdesign未設定保持、R5のREPUBLISH_ON_BOOTによる既定OFFをコードで確認。Studio43件などは今回すべて独立再実行したものではない。
- 公開HTMLバックアップ・復元APIへローカルのHTTPリクエストを送り、以下の2件を再現。本番への接続・変更はしていない。

## R1 / P1：全件バックアップの復元が、対象外サイトの新しい公開を上書きする

対象: `app/api/admin/published-html-backup/route.ts:81`、`docs/rebuild-migration-2026-09-11.md` のバックアップ・復元手順。

現在の手順は全件バックアップを取得し、1件・少数ずつ再生成し、戻す時にバックアップをそのまま全件POSTする。APIも各idへ無条件でpublished_htmlを書き戻す。

ローカルで、バックアップ取得後にBサイトだけ別の新しいHTMLへ公開し、保存済みバックアップをPOSTした。結果は `restored:4, failed:[]` で、Bの新しい公開まで古いHTMLへ戻った。

再生成した対象IDとその処理が書いた版・ハッシュを記録し、復元もその対象だけに限定する。その後に利用者の再公開が入った行は競合として止める。バックアップ全体を無条件に流し込む標準手順はやめる。少なくとも「Aだけ再生成→Bは利用者が新しく公開→Aだけ戻す」でBを保持する試験を追加する。

## R2 / P2：存在しないサイトの復元を成功と数える

対象: `app/api/admin/published-html-backup/route.ts:83`〜86。

存在しないidを1件POSTすると、ローカルで `restored:1,total:1,failed:[]` が返った。PostgRESTの更新0件はerrorにならないため、errorだけを見ると誤判定になる。

更新した行をselect等で取得し、1件更新できた場合だけ成功と数える。削除済みIDはnot_foundとして返す。再生成側の同様の更新判定も確認する。dryRunも入力配列を返すだけではなく、現在の対象と競合を確認できる形にする。

## 公開計画：実際に出る46コミットを基準にする

今回、`git ls-remote origin refs/heads/main` でGitHub mainが `0d00dfe82b3688dfafb03058cca0e1e560ce4165` のままであることを確認した。ローカルmainはそこから28コミット先、刷新ブランチはさらに18コミット先。

「刷新差分にはSQL追加がない」は確認範囲を限定した説明。実際の公開には前段の独自ドメインのSQL/RPCやDOMAIN_PROBE_SECRET等の前提も含まれる。GitHub mainとRenderの稼働コミットは別なので、出荷前には実デプロイ対象SHAも確認する。

前段を含む差分・依存するSQL/RPC・必要設定・既存顧客への影響を一覧にし、未適用の機能を含めるか、先行公開から分離するかを決める。必要設定は値をログへ出さず、有無と対応だけ確認する。今回のレビューでは本番SQL・push・DNS変更・デプロイを承認・実施していない。

## デザインの仕上げ（素材制作と同時進行）

LARUHPの冒頭に実物が出る方向は採用。ただしスマホのデモ枠は `aspect-[390/2974]` なので、幅358pxなら約2730pxになる。料金・予約フォームまで続き、雰囲気／書体の操作が枠の後に来る。自社サービスの料金へ行く前に架空サロンの予約画面が長く続く構成になっている。

冒頭は完成ヒーローと組立の見せ場をコンパクトに示し、操作ボタンを近くに置く。フォームまでのフル試用は「実際に触ってみる」等から明示的に展開できる形に分ける。スクロールを奪わず、デモ内の架空サロンの予約とLARUHPの申込を区別する。

雰囲気選択の提出画像では下段3案が白く写っている。画像だけで実装不具合とは断定しない。各案を画面内にスクロールして読み込み後に確認し、最終画像は全部の見本を確認できる形で提出する。

## 書体・性能・素材

- 端末の和文書体を当面採用。専用サブセットの作成を出荷の前提にしない。読みにくい端末があればウェイト・行送りを実機で調整する。
- 報告のLCPはローカル、外部タグ遮断、会社ヒーロー素材未投入の条件。性能改善の比較値として扱い、本番全体が1.1秒と表現しない。
- 計測スクリプトは親ページでの観測なので、素材差替え後にはデモ写真の表示と操作可能になる時間も別途記録する。
- Higgsfieldは会社の主役静止画→動画、結い庵のスタイル4枚・架空スタッフ3枚・B案動画の制作を進める。LARUHP用の抽象動画は不要。
- 次回は復元の修正、46コミットを対象にした出荷計画、素材入りプレビューをまとめて提出する。細かい見た目の調整ごとの承認待ちは不要。
