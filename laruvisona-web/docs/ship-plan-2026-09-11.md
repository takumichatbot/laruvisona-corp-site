# 出荷計画 — GitHub main からの46コミットを対象に（2026-09-11）

これまでの記録は「今回の刷新18コミット」だけを見ていた。
実際に本番へ出るのは **GitHub main からの差分すべて**なので、その前提で作り直した。

> **この文書は計画で、実施記録ではない。**
> push・本番SQL・DNS変更・デプロイは、まだ何も行っていない。

## 1. いま、どこに何があるか

| | コミット | 内容 |
|---|---|---|
| GitHub `origin/main` | `0d00dfe` | いま公開されているはずのもの |
| ローカル `main` | `0d00dfe` +28 | 独自ドメイン・基準作品・公開HTMLの修正（未push） |
| `rebuild-2026-09` | さらに +18 | 今回の刷新（未push） |
| Render の稼働コミット | **未確認** | 下の「出す前に確かめること」参照 |

`origin/main` と Render の稼働コミットは別物。Render が `0d00dfe` を動かしている
とは限らないので、出荷前に実際のデプロイ対象SHAを確かめる。

### 公開HTMLの版

| | `EXPORT_VERSION` |
|---|---|
| `origin/main` | **3** |
| ローカル `main` | 7 |
| `rebuild-2026-09` | 9 |

**ここが今回いちばん大きい。** 既存顧客の公開HTMLは、いま v3 で書かれている。
作り直すと v3 → v9 へ一気に進む。前の記録は「7 → 9」と書いていたが、
それは「今回の刷新ぶん」だけを見た数字だった。

## 2. 出す単位を3つに分ける

### A. 独自ドメイン（12コミット）

`3097db2` `ecea9d0` `4cee5c0` `ce05e76` `608a4c4` `c82ddda` `8076627` `b4d53ea`
`7ca186d` `39c23a5` `6a68f75` `435fe95`（最後の1つは調査記録のみ）

| | |
|---|---|
| **SQL** | `supabase/site_domains.sql`（**未適用**）。表2つ・RLS・トリガ2つ・状態遷移のRPC 8つ |
| **必要な設定** | `DOMAIN_PROBE_SECRET`（**新規・必須**）／`RENDER_API_KEY`・`RENDER_SERVICE_ID`・`RENDER_SERVICE_SLUG`・`RENDER_APEX_IP`（origin/main にも参照があるので既存のはず） |
| **既存顧客への影響** | `sites` に `guard_sites_custom_domain` トリガが付く。利用者の権限（authenticated）から `custom_domain` を直接書き換えられなくなる。編集画面の保存は `custom_domain` を送らないので通る |
| **SQLを当てないとどうなるか** | 壊れない。表が無い場合は `app/api/sites/[id]/domain/route.ts` が `migrationPending` を返し、既存の接続済みドメインは `legacy` として画面に出る。`proxy.ts` の別名判定も引けなければ「知らないホスト」として素通しする（コードで確認済み） |
| **当てないと使えないこと** | 新しい独自ドメインの追加・所有確認・apex/wwwの主従・別名の308転送 |

適用手順と戻し方は `supabase/site_domains_runbook.md` にある。
`site_domains_permission_check.sql` / `site_domains_regression.sql` /
`test-bootstrap.sql` は**検証用で、本番では実行しない**。

### B. 基準作品・公開HTML・書体・記録（16コミット）

`5348247` `9284653` `6e73f47` `e6893ed` `510d888` `576d5e5` `ac31388` `d1ce3bc`
`e9aa73d` `3f8a441` `f9a49ee`（作品と公開HTML）
`c29b64e` `ac7ccbf` `89037d4`（書体・その計測）
`18cd98a` `e836ed3`（記録・検証データ）

| | |
|---|---|
| **SQL** | 無し |
| **必要な設定** | 無し（既存のまま） |
| **既存顧客への影響** | 公開HTMLを作り直したときだけ変わる。v3 → v7 で変わるのは次の4点 |

1. `ac31388` 動きを「なし」にしても、写真・料金・質問が隠れたままにならない
2. `576d5e5` シンプル予約が実際に送れる。メニューからの引き継ぎ。固定CTA
3. `d1ce3bc` ヒーロー写真の配信（avif/webp/サイズ別）。固定CTAがCookie帯に隠れない
4. `e9aa73d` 予約・問い合わせの必須項目を実APIと揃える（メール必須）。見出しの改行

**どれも既存サイトの見え方が変わりうる。** 特に 2・4 はフォームの項目が変わる。

### C. 今回の刷新（18コミット）

`9e687dc` 〜 `a7d22b8`。内容と戻し方は `docs/rebuild-migration-2026-09-11.md`。

| | |
|---|---|
| **SQL** | 無し |
| **必要な設定** | `REPUBLISH_ON_BOOT`（**新規・任意**）。設定しなければ起動時に作り直さない |
| **既存顧客への影響** | 公開HTMLを作り直したときだけ。v7 → v9 の変更点は移行の記録にある |

## 3. 出す順（案）

前提として、**コードのデプロイと、顧客HTMLの作り直しは別の操作**になっている
（`REPUBLISH_ON_BOOT` を立てなければ、デプロイしても顧客サイトは1行も書かれない）。

### 案1: 一度に出して、作り直しだけ段階的にやる（おすすめ）

1. 46コミットをまとめて `main` へ入れて push → デプロイ
   - `REPUBLISH_ON_BOOT` は設定しない
   - `DOMAIN_PROBE_SECRET` は、独自ドメインを使うなら先に設定する
2. 自社の2ページと制作画面を確かめる（顧客サイトはまだ v3 のまま）
3. `site_domains.sql` を適用する（runbook のとおり）
4. 顧客サイトの作り直しを、1件 → 5件 → 25件 → 全件（`docs/rebuild-migration-2026-09-11.md` の段階3）

**利点**: ブランチを分けて持ち続けなくてよい。作り直しを止めれば、顧客の見え方は
いつでも v3 のままにできる。
**注意**: コードは46コミットぶん一気に変わる。自社ページ・管理画面・制作画面は
その場で新しくなる。

### 案2: 独自ドメインだけ後にする

`main` から独自ドメインの12コミットを外したブランチを作って先に出し、
`site_domains.sql` の適用と一緒に後から出す。

**利点**: SQLの適用とコードの反映を完全に揃えられる。
**注意**: 12コミットは他の変更と重なる部分（`proxy.ts`・`lib/public-site-url.ts`）が
あるので、外すと手作業の解消が要る。**コードが壊れないことを確認済みなので、
この手間に見合うかは疑問**（判断は齋藤さん）。

## 4. 出す前に確かめること（値は出さない）

```bash
# 1. Render が実際に動かしているコミット（管理画面の Deploys、または）
#    値ではなくSHAだけを見る
git ls-remote origin refs/heads/main

# 2. 必要な設定が「ある」かどうかだけを見る。値は表示しない
#    Render の Environment 画面で、次のキーの有無を目視する:
#      DOMAIN_PROBE_SECRET   … 独自ドメインを出すなら必須（新規）
#      RENDER_API_KEY / RENDER_SERVICE_ID / RENDER_SERVICE_SLUG / RENDER_APEX_IP
#      ADMIN_SECRET          … 作り直しAPIの合言葉（既存）
#      REPUBLISH_ON_BOOT     … **設定しない**（設定すると起動時に全件作り直す）

# 3. site_domains がすでに当たっているか（表の有無だけ）
#    Supabase SQL Editor で:
#      select to_regclass('public.site_domains') is not null as applied;

# 4. いま顧客サイトが何版で書かれているか（書かない）
curl -X POST -H "Authorization: Bearer $ADMIN_SECRET" -H 'content-type: application/json' \
  -d '{"dryRun":true}' https://laruvisona.jp/api/admin/republish-all
#    → 対象ごとに outdated（版が古いか）と current_sha256 が返る
```

シェルの履歴やログに値そのものを残さないこと。
`DOMAIN_PROBE_SECRET` は到達確認の署名鍵で、漏れると確認を偽装できる。

## 5. 戻し方

| 戻すもの | やり方 |
|---|---|
| コード | `git revert <コミット>`（一覧は `docs/rebuild-migration-2026-09-11.md`） |
| 顧客の公開HTML | 作り直しの応答（`undo` つき）を書き戻す。**その回が書いた分だけ**戻る |
| `site_domains.sql` | `supabase/site_domains_runbook.md` の戻し手順 |
| 起動時の作り直し | `REPUBLISH_ON_BOOT` を外す（既定で走らない） |

**コードを戻しても、すでに書かれた公開HTMLは戻らない。**
版数（`EXPORT_VERSION`）を下げても同じで、戻すのは書き戻しだけ。

## 6. まだ確かめていないこと（正直に）

- **Render の稼働コミット**。この環境から Render の管理画面・APIへは出られない
- **本番Supabaseに `site_domains` が当たっているか**。本番DBへは接続していない
- **本番の設定値の有無**。Render の Environment は見ていない
- `site_domains.sql` の実効権限とトリガは、一時的な実PostgreSQL上での回帰
  （`./supabase/run-sql-regression.sh`）までは確認済み。**本番の権限設定と同一である保証は無い**
- 同時保存の13件は、ローカルのPostgREST代替で確認したもの。
  **本番PostgreSQLの実効権限・トリガまでを保証する試験ではない**
